#include <iostream>

using namespace std;

int main() {

	int a, b;
	cin >> a >> b;

	if (b > a) {
		int c = b;
		b = a;
		a = c;
	}

	int r = a - (a / b) * b;
	int r1 = b - (b / r1) * r1;

	cout << r << r1;

	return 0;
}
